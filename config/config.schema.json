{
  "title": "LeviVision Config",
  "description": "Persistent settings for LeviVision (Night Vision / X-Ray).",
  "type": "object",
  "properties": {
    "version": {
      "type": "integer",
      "title": "Version",
      "readOnly": true,
      "default": 1
    },
    "nightVision": {
      "type": "boolean",
      "title": "Night Vision",
      "description": "Preference toggle. Pick the matching variant via the gear icon next to RedstoneTechShader in Global Resources.",
      "default": false
    },
    "xray": {
      "type": "boolean",
      "title": "X-Ray",
      "description": "Preference toggle. Pick the matching variant via the gear icon next to RedstoneTechShader in Global Resources.",
      "default": false
    }
  }
}
